#include <stdio.h>
#include <stdlib.h>

int main()
{
  system("echo hello world hello guys!");
  system("echo in todays sharing im gonna show off");
  system("echo how we can actually encrypt our");
  system("echo shell script. using C programming.");
  system("echo ");
  system("echo BTW what youre seeing right now is");
  system("echo i am using tmux.");
  system("echo tmux is a great program which manage ");
  system("echo your terminal tab. i use it for");
  system("echo everyday.");
  system("echo ");
  system("echo okay now lets start!");
  system("echo the point of this way of encrypting");
  system("echo is that we create a C file and fill it up");
  system("echo with shell scripts. so then we can compile  it");
  system("echo and make it unreadable for humans!");
  system("echo ");
  system("echo unless youre an android that came from the future");
  system("echo okay lets start create a C file");

  system("echo now we have created a c file");
  system("echo im gonna using these echoes to fill");
  system("echo my c file");
  return 0;
}
