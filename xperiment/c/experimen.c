#include <stdio.h>
#include <stdlib.h>

int main()
{
  system("echo halo guys");
  system("echo in todays sharing ");
  system("echo i wan to show off how ");
  system("echo we can actually encrypt out");
  system("echo shell scripts easily");
  system("echo using C programming!");
  system("echo ");
  system("echo lets call it a shell script inside");
  system("echo a C program, so we can then compile it");
  system("echo and make it unreadable for humans!");
  system("echo ");
  system("echo okay lets start!");

  system("echo lets first create a C file");
  system("echo using vim is the best way");
  system("echo because vim is the best text editor!");
  system("echo now lets begin!");
  return 0;
}
